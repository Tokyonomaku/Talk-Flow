{
  "version": 2,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "cd frontend && npm run build && cp -r build/* ../",
  "outputDirectory": ".",
  "framework": "create-react-app"
}
